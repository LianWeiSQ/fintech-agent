# Audit Note Format

Audit notes should stay machine-readable and stable:
- prefix the reason class
- keep the assessment id or event id when available
- include score and confidence context when relevant
